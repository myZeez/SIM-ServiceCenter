<?php require "vendor/autoload.php"; $app = require_once "bootstrap/app.php"; $app->make("Illuminate\Contracts\Console\Kernel")->bootstrap(); $s = App\Models\Symptom::where("code", "AIOG001")->first(); if($s) { echo "Symptom ID: " . $s->id . "\n"; $rules = App\Models\Rule::where("symptom_id", $s->id)->count(); echo "Rules count: " . $rules . "\n"; } else { echo "AIOG001 not found\n"; }
